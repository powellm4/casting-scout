[project]
name = "casting-scout"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: hits real external sites (deselect with '-m not integration')",
]
